# Core dependencies
fastapi==0.116.2
uvicorn==0.23.2
pydantic==2.6.0
pyyaml==6.0.1
requests==2.32.3
defusedxml==0.7.1
urllib3==2.5.0

# Monitoring
loguru==0.7.2

# Testing
pytest==7.4.3
httpx==0.25.1

# Development
black==24.3.0
isort==5.12.0
flake8==6.1.0

# Additional dependencies
z3-solver==4.12.2.0
docker==6.1.3
networkx==3.1
flask==3.1.2
flask-cors==6.0.1
optuna==3.4.0
python-ldap==3.4.3
# SECURITY WARNING: MLflow has known CVEs (CVE-2024-37052 to CVE-2024-37060)
# related to arbitrary code execution via unsafe deserialization when loading models.
# These affect all versions from their respective introduction points with no fix available yet.
# Vulnerabilities affect:
# - PyFunc models (CVE-2024-37054) - affects >= 0.9.0
# - Recipe execution (CVE-2024-37060) - affects >= 1.27.0
# - LightGBM models (CVE-2024-37056) - affects >= 1.23.0
# - TensorFlow models (CVE-2024-37057) - affects >= 2.0.0rc0
# - pmdarima models (CVE-2024-37055) - affects >= 1.24.0
# - scikit-learn models (CVE-2024-37052, CVE-2024-37053) - affects >= 1.1.0
# - PyTorch models (CVE-2024-37059) - affects >= 0.5.0
#
# CRITICAL MITIGATION STEPS (MUST BE IMPLEMENTED):
# 1. Only load models from trusted sources (internal model registry)
# 2. Run MLflow server with authentication and restricted network access
# 3. Use model signatures and strict input validation
# 4. Run model serving in isolated containers/sandboxes
# 5. Disable model loading from external sources
# 6. Monitor MLflow GitHub for security patches - no fix available as of Sept 2025
# 7. Consider using alternative model serving solutions for untrusted models
mlflow==3.3.2
setuptools==80.9.0
pandas==2.2.0
geopy==2.4.1
flask-socketio==5.3.6
wandb==0.21.1
seaborn==0.13.2
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0
transformers==4.55.4